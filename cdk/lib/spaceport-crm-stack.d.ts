import * as cdk from "aws-cdk-lib";
import type { Construct } from "constructs";
export declare class SpaceportCrmStack extends cdk.Stack {
    constructor(scope: Construct, id: string, props?: cdk.StackProps);
}
